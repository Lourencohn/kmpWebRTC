{
  "name": "trovatacast-buyer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Web app do cliente (buyer) do TrovataCast — abre via link, sem instalar.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 5174",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^1.2.0",
    "@vitest/coverage-v8": "^2.1.8",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "vite": "^8.0.14",
    "vitest": "^2.1.8"
  }
}
